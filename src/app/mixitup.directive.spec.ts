import { MixitupDirective } from './mixitup.directive';

describe('MixitupDirective', () => {
  it('should create an instance', () => {
    const directive = new MixitupDirective();
    expect(directive).toBeTruthy();
  });
});
